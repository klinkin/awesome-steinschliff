# CHANGELOG

## 1.0.0

- toolchain: единый стек `uv` + `just`, полностью воспроизводимые команды
- quality: единый форматтер `ruff format`, включён `PTH` (pathlib-first), строгий pytest
- docs: `mkdocs --strict` проверяется в CI, исправлены ссылки/якоря


